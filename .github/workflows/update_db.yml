name: Sincronizar Pipz com Google Cloud

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    environment: PIPZ
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Autenticar no Google Cloud
        uses: google-github-actions/auth@v2 # Versão atualizada
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Iniciar Cloud SQL Proxy
        uses: google-github-actions/setup-cloudsql-connect@v1
        with:
          instance_connection_name: "caldeira-lake:southamerica-east1:banco-de-dados-caldeira-campus" # <--- TROQUE PELO SEU

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Dependências
        run: pip install -r requirements.txt

      - name: Executar Script
        env:
          PIPZ_TOKEN: ${{ secrets.PIPZ_TOKEN }}
          # Agora o localhost vai funcionar porque o Proxy está ativo!
          DB_URL: ${{ secrets.DB_URL }}
        run: python main.py